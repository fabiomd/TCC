;;;; wao.lisp

(ql:quickload :parse-js)
(in-package #:wao)

;;; "wao" goes here. Hacks and glory await!

(defvar *code* nil)

(defun wao (jsfile)
  (in-package :parse-js)
  (setf *code* (parse-js "var x=5"))
  (in-package #:wao))
