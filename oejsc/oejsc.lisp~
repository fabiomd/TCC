;;;; oejsc.lisp

(ql:quickload :parse-js)
(in-package #:oejsc)

;;; "oejsc" goes here. Hacks and glory await!

(defvar *code* nil)

(defun oejsc (jsfile)
  (in-package :parse-js)
  (setf *code* (parse-js "var x=5"))
  (in-package #:oejsc))
